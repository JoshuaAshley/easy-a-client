<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AppCompat.SplashScreen" parent="Theme.AppCompat.NoActionBar">
        <!-- Splash screen background color -->
        <item name="android:windowSplashScreenBackground">@color/easy_a_blue</item>

        <!-- Splash screen animated icon -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_easy_a_launcher_title</item>

        <!-- Splash screen animation duration -->
        <item name="android:windowSplashScreenAnimationDuration">4000</item>
    </style>
</resources>