<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Easy_A"
        tools:targetApi="31">

        <!-- SplashActivity is the launcher -->
        <activity
            android:name=".controllers.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainScreen -->
        <activity android:name=".controllers.MainScreen" android:exported="true" />

        <!-- LoginScreen -->
        <activity android:name=".controllers.LoginScreen" />
    </application>
</manifest>
